const Array = [];

for (let i = 0; i < 5; i++) {
  Array.push(i);
}
console.log(Array);

const arr = [-2,6,-7,8,10,12,14,18,-1];
function sortArr(num){
    for (let i=0; i< arr.length; i++){
        num = arr[i];
    const newArr = [];
        if (num > 0){
            newArr.push(num);
            
        }
    }
    return newArr;
}